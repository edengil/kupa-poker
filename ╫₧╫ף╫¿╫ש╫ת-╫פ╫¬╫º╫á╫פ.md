# מדריך התקנה — צעד אחר צעד

המדריך מניח שלא עשית את זה קודם. אין שלב שדורש ידע מוקדם, וכל שלב אומר גם מה
אתה אמור לראות על המסך כדי לדעת שהצליח.

**זמן:** בערך 40 דקות בפעם הראשונה. **עלות:** אפס, הכל בתוכניות החינמיות.

---

## קודם כל — למה יש שלושה אתרים

זה החלק שהכי מבלבל בהתחלה, אז שווה להבין אותו לפני שנוגעים בכלום. אתה עומד
לפתוח חשבון בשלושה מקומות, וכל אחד עושה דבר אחר:

| האתר | התפקיד שלו | למה בלעדיו לא הולך |
|---|---|---|
| **Supabase** | מסד הנתונים | כאן יישבו הערבים, השחקנים והסכומים. זה מה שמחליף את הזיכרון של הדפדפן |
| **Google Cloud** | אישור להתחברות | גוגל לא נותנת לאתר אקראי לבקש "התחבר עם Google". צריך לרשום אצלה את האתר פעם אחת |
| **Vercel** | האחסון של האתר | כאן האתר באמת יושב ומקבל כתובת שאפשר לשלוח |

הסיבה שקופצים ביניהם היא ש-Supabase וגוגל צריכים להכיר אחד את השני: אתה מעתיק
קוד מגוגל ל-Supabase, ואחר כך כתובת מ-Supabase בחזרה לגוגל. זה הכל.

**מה תעתיק במהלך הדרך.** פתח פנקס ריק ותשמור שם ארבעה דברים כשתגיע אליהם:

```
1. Project URL        (משלב 1.4)
2. anon public key     (משלב 1.4)
3. Client ID           (משלב 2.5)
4. Client Secret       (משלב 2.5)
```

---

# חלק 1 · Supabase

### 1.1 · פתיחת חשבון

לך ל-**supabase.com** ולחץ **Start your project**. התחבר עם GitHub או עם Google.
אם אין לך GitHub — תצטרך אותו בהמשך בשביל Vercel, אז זה זמן טוב לפתוח.

### 1.2 · יצירת פרויקט

לחץ **New project**.

- **Name** — `kupa-poker`
- **Database Password** — לחץ **Generate a password** ושמור אותו בפנקס.
  לא תצטרך אותו למדריך הזה, אבל אין דרך לראות אותו שוב.
- **Region** — `Central EU (Frankfurt)`. הכי קרוב לישראל
- **Plan** — Free

**אין במסך הזה אפשרות לבחור Free — וזה תקין.** התוכנית ב-Supabase נקבעת ברמת
**הארגון**, לא ברמת הפרויקט. חשבון חדש נפתח אוטומטית בארגון Free, וכל פרויקט
בתוכו יהיה Free. לבדיקה אחר כך: תפריט הצד → הארגון → **Billing**, אמור לכתוב שם
Free.

### 1.2ב · מסך ה-Security

באותו טופס, קצת למטה, יש שלוש תיבות סימון:

| התיבה | מה לעשות | למה |
|---|---|---|
| **Enable Data API** | ✅ **השאר מסומן** | בלעדיה האפליקציה לא יכולה לדבר עם המסד מהדפדפן בכלל |
| **Automatically expose new tables** | ⬜ **הסר את הסימון** | Supabase עצמה ממליצה. ה-`schema.sql` נותן את ההרשאות במפורש, אז אתה לא מפסיד כלום |
| **Enable automatic RLS** | ✅ **סמן** | רשת ביטחון. הסכימה מפעילה RLS בעצמה, אבל אין סיבה לוותר |

> אם כבר יצרת את הפרויקט עם ההגדרות המקוריות — לא נורא, הכל יעבוד גם ככה.
> הסכימה בנויה לשני המצבים.

לחץ **Create new project**. ההקמה לוקחת דקה-שתיים.

> אתה אמור לראות מסך פרויקט עם תפריט צד שחור.

### 1.3 · יצירת הטבלאות

בתפריט הצד לחץ על **SQL Editor** (אייקון של מסמך עם `>_`), ואז **New query**.

פתח את הקובץ `supabase/schema.sql` מהפרויקט שהורדת, **העתק את כולו**, הדבק
בחלון הגדול, ולחץ **Run** (או Ctrl+Enter).

> אתה אמור לראות למטה `Success. No rows returned`. זה נכון — הפקודות יוצרות
> טבלה, הן לא מחזירות נתונים.

לבדיקה: בתפריט הצד → **Table Editor**. אמורה להופיע טבלה בשם `groups`, ריקה,
עם עמודות `id`, `owner_id`, `slug`, `name`, `data`, `config`, `live`.

### 1.4 · העתקת שני המפתחות

**Project Settings** מפוצל לשני מסכים, אז צריך לעבור בשניהם:

**א. Project Settings → Data API** → העתק את **Project URL**.
נראה כמו `https://abcdefghijk.supabase.co`.

> קיצור דרך: המזהה מופיע גם בכתובת של הדפדפן —
> `supabase.com/dashboard/project/abcdefghijk`.

**ב. Project Settings → API Keys** → תחת **Publishable key** לחץ על אייקון
ההעתקה ליד `sb_publis…`. זה המפתח.

> ⚠️ באותו מסך יש גם **Secret keys** (`sb_secret_…`). אותו **לעולם לא** לוקחים.
> הוא עוקף את כל ההגנות ואסור שיגיע לדפדפן.

**אם אתה רואה לשונית "Legacy anon, service_role API keys"** — אפשר להתעלם ממנה.
המפתח הפומבי החדש עושה בדיוק את אותו דבר, והישנים בדרך להוצאה משימוש בסוף 2026.

---

# חלק 2 · Google

השלב הזה הוא היחיד שבאמת מייגע. אתה מבקש מגוגל אישור שהאתר שלך רשאי להציג
"התחבר עם Google". עושים אותו פעם אחת.

### 2.1 · פרויקט חדש

לך ל-**console.cloud.google.com**. למעלה, ליד הלוגו, יש בורר פרויקטים —
לחץ עליו ואז **New Project**. קרא לו `kupa-poker` ולחץ **Create**.

חכה שההודעה תיעלם, ואז **ודא שהפרויקט החדש נבחר בבורר למעלה**. זו הטעות
הכי נפוצה כאן — להגדיר הכל בתוך פרויקט אחר.

### 2.2 · מסך ההסכמה

בחיפוש למעלה הקלד `OAuth` ובחר **OAuth consent screen** (או Google Auth Platform).

- **User Type** → **External** → Create
- **App name** — `קופה — פוקר`
- **User support email** — המייל שלך
- **Developer contact** — המייל שלך

שמור והמשך עד הסוף. את מסכי **Scopes** ו-**Test users** אפשר לעבור בלי לגעת.

> אם תישאל על **Publishing status**, השאר ב-**Testing**. במצב הזה רק מיילים
> שהוספת כ-Test users יכולים להתחבר. הוסף את `edengil94@gmail.com` תחת
> **Test users**, אחרת אתה עצמך לא תוכל להיכנס.
>
> זה בסדר גמור — רק **אתה** מתחבר לאפליקציה. החברים פותחים לינק צפייה בלי
> חשבון בכלל, וזה לא עובר דרך גוגל.

### 2.3 · יצירת פרטי הגישה

בתפריט → **Credentials** → למעלה **+ Create Credentials** → **OAuth client ID**.

- **Application type** → **Web application**
- **Name** → `kupa-web`

**אל תלחץ Create עדיין.** חסר שדה אחד, ואנחנו הולכים להביא אותו מ-Supabase.

### 2.4 · הבאת כתובת החזרה מ-Supabase

פתח **לשונית חדשה** עם Supabase (אל תסגור את גוגל).

בתפריט הצד → **Authentication** → **Providers** (בגרסאות מסוימות: Sign In / Up)
→ מצא **Google** ברשימה ולחץ עליו.

בתוך הפאנל שנפתח יש שדה בשם **Callback URL (for OAuth)** עם כתובת שנראית כך:

```
https://abcdefghijk.supabase.co/auth/v1/callback
```

**העתק אותה.**

### 2.5 · השלמת היצירה בגוגל

חזור ללשונית של גוגל. תחת **Authorized redirect URIs** לחץ **+ ADD URI**
והדבק את הכתובת שהעתקת. עכשיו לחץ **Create**.

> יקפוץ חלון עם **Client ID** ו-**Client Secret**. העתק את שניהם לפנקס.
> את ה-Secret אפשר לראות שוב אחר כך, אבל עדיף לא להסתמך על זה.

---

# חלק 3 · חיבור השניים

חזור ללשונית של Supabase, לאותו פאנל של **Google** תחת Authentication →
Providers.

1. הפעל את המתג **Enable Sign in with Google**
2. **Client ID** — הדבק מהפנקס
3. **Client Secret** — הדבק מהפנקס
4. לחץ **Save**

> החיבור סגור. גוגל יודעת מי Supabase, ו-Supabase יודעת מי גוגל.

---

# חלק 4 · הרצה במחשב שלך

עכשיו חוזרים לקוד. פתח טרמינל בתיקייה של הפרויקט.

### 4.1 · קובץ המפתחות

בתיקייה יש קובץ בשם `.env.local.example`. צור ממנו עותק בשם `.env.local`:

```bash
cp .env.local.example .env.local
```

פתח את `.env.local` בעורך ומלא את שלוש השורות מהפנקס:

```
NEXT_PUBLIC_SUPABASE_URL=https://abcdefghijk.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

בלי גרשיים ובלי רווחים סביב סימן השווה.

> הקובץ הזה נשאר רק אצלך. הוא כבר רשום ב-`.gitignore` ולא יעלה ל-GitHub.

### 4.2 · הרצה

```bash
npm install
npm run dev
```

פתח בדפדפן **http://localhost:3000**.

> אתה אמור לראות מסך ירוק עם ♠ וכפתור **התחברות עם Google**. לחץ עליו,
> בחר את החשבון, ותחזור לאפליקציה עם הטבלה שלך.

אם הגעת לכאן — הכל עובד. נשאר רק להעלות לאוויר.

---

# חלק 4ב · מסלול בלי מחשב (דפדפן בלבד)

אם אין לך מחשב זמין — אפשר לדלג על חלק 4 לגמרי. `npm install` ירוץ אצל Vercel
בענן, לא אצלך. מה שנשאר זה להביא את הקבצים ל-GitHub.

### 4ב.1 · פתיחת ה-zip בטלפון

הורד את `poker-cloud.zip`, פתח את אפליקציית **Files**, מצא אותו ב-**Downloads**
ולחץ עליו פעם אחת. iOS יפרוס אותו לתיקייה בשם `poker-cloud`.

### 4ב.2 · יצירת ה-repo

ב-**github.com** → **+** למעלה → **New repository**

- **Repository name** — `kupa-poker`
- **Private**
- **אל תסמן** "Add a README file" — עדיף שה-repo יתחיל ריק

**Create repository**

### 4ב.3 · העלאת הקבצים

במסך הריק שנפתח לחץ **uploading an existing file**.

**קודם נסה את הדרך הקצרה:** בבורר הקבצים בחר **Browse** ונסה לבחור את התיקייה
`poker-cloud` כולה. אם iOS נותן לך לבחור תיקייה — הכל עולה במכה אחת עם המבנה,
וסיימת. גלול למטה, לחץ **Commit changes**, ועבור לחלק 5.

**אם זה לא עבד** — מעלים תיקייה-תיקייה. בכל סבב: לחץ **Add file** →
**Upload files**, ובשדה הנתיב למעלה הקלד את שם התיקייה עם `/` בסוף. GitHub
ייצור אותה לבד.

| נתיב להקליד | קבצים לבחור |
|---|---|
| *(שורש, בלי נתיב)* | `package.json`, `package-lock.json`, `next.config.mjs`, `jsconfig.json`, `vercel.json` |
| `app/` | `layout.jsx`, `page.jsx` |
| `app/g/[slug]/` | `page.jsx` |
| `app/auth/callback/` | `route.js` |
| `app/api/keep-alive/` | `route.js` |
| `components/` | `PokerApp.jsx`, `OwnerApp.jsx`, `PublicApp.jsx`, `LiveNow.jsx` |
| `lib/` | `store.js`, `realtime.js`, `supabaseClient.js`, `supabaseServer.js` |

אחרי כל תיקייה — **Commit changes**. שבעה סבבים, ואפשר לעצור באמצע ולחזור.

> **מה לא צריך להעלות:** `.gitignore` ו-`.env.local.example` (iOS מסתיר קבצים
> שמתחילים בנקודה, והם לא נחוצים לבנייה), וגם `README.md`, `מדריך-התקנה.md`
> ו-`supabase/schema.sql` — את ה-SQL כבר הרצת, והשאר זה תיעוד.

> **שים לב לסוגריים** ב-`app/g/[slug]/`. הם חלק מהשם ולא טעות — ככה Next.js
> מסמן נתיב דינמי.

### 4ב.4 · בדיקה לפני שממשיכים

בדף הראשי של ה-repo אתה אמור לראות ארבע תיקיות — `app`, `components`, `lib` —
וחמישה קבצים בשורש. אם `app` מכילה רק קובץ אחד, חסר לך משהו: היכנס אליה
ובדוק מול הטבלה למעלה.

---

# חלק 5 · העלאה ל-Vercel

### 5.1 · העלאת הקוד ל-GitHub

**אם הגעת מחלק 4ב** — כבר עשית את זה, דלג ל-5.2.

ב-**github.com** → **New repository** → שם `kupa-poker` → **Private** → Create.
GitHub יראה לך פקודות. מהתיקייה של הפרויקט:

```bash
git init
git add .
git commit -m "kupa poker cloud"
git branch -M main
git remote add origin https://github.com/<שם-המשתמש-שלך>/kupa-poker.git
git push -u origin main
```

### 5.2 · חיבור Vercel

לך ל-**vercel.com**, התחבר עם GitHub, ולחץ **Add New → Project**. בחר את
`kupa-poker` ולחץ **Import**.

**לפני שאתה לוחץ Deploy**, פתח את **Environment Variables** והוסף שלושה:

| Name | Value |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | ה-Project URL מהפנקס |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | ה-anon key מהפנקס |
| `NEXT_PUBLIC_SITE_URL` | `https://kupa-poker.vercel.app` |

הערך השלישי הוא הכתובת שתקבל. Vercel מציג אותה במסך; אם היא שונה, השתמש
בשלו. אפשר גם לתקן אחרי הדיפלוי הראשון.

לחץ **Deploy** וחכה כדקה.

### 5.3 · סגירת המעגל — השלב שהכי קל לשכוח

הכתובת השתנתה מ-`localhost` לדומיין אמיתי, ו-Supabase עדיין לא מכירה אותו.
בלי השלב הזה **ההתחברות תיכשל**, וכנראה בלי הודעה מובנת.

חזור ל-Supabase → **Authentication** → **URL Configuration**:

- **Site URL** → `https://kupa-poker.vercel.app`
- תחת **Redirect URLs** לחץ **Add URL** והוסף:
  `https://kupa-poker.vercel.app/auth/callback`

לחץ **Save**.

> אם שינית את `NEXT_PUBLIC_SITE_URL` אחרי הדיפלוי, לך ב-Vercel ל-
> **Deployments** → הנקודות ליד האחרון → **Redeploy**. משתני סביבה נכנסים
> לתוקף רק בבנייה חדשה.

---

# חלק 6 · העברת הנתונים הקיימים

הנתונים שלך יושבים עכשיו בדפדפן של המכשיר שבו השתמשת באפליקציה הישנה.

1. פתח את **האפליקציה הישנה** באותו מכשיר
2. לך למסך **גיבוי** ולחץ **ייצוא** — יירד קובץ `.json`
3. פתח את **האפליקציה החדשה** בכתובת של Vercel, התחבר עם Google
4. באותו מסך גיבוי — **ייבוא**, ובחר את הקובץ

> הטבלה אמורה להתמלא. מאותו רגע הנתונים יושבים ב-Supabase, לא בדפדפן.

**אל תמחק את האפליקציה הישנה עד שווידאת שהכל עבר.**

---

# חלק 7 · בדיקה שהכל באמת עובד

1. באפליקציה, למעלה, לחץ **שליחת לינק לצפייה**. הלינק יועתק או ייפתח בשיתוף
2. פתח את הלינק **בחלון גלישה פרטית** — שם אתה לא מחובר, בדיוק כמו חבר
3. אמור להופיע **צפייה בלבד** למעלה, ורק שני טאבים: טבלה ושחקנים
4. השאר את החלון הפרטי פתוח, וחזור לחלון הרגיל. לך ללייב והוסף שחקן
5. הסתכל על החלון הפרטי — תוך שנייה-שתיים אמור לצוץ שם פאנל **משחק עכשיו**

אם זה קרה — סיימת. שלח את הלינק לקבוצה.

---

# מה עושים כשמשהו לא עובד

**"redirect_uri_mismatch" בזמן התחברות**
הכתובת ב-Google Cloud → Credentials → Authorized redirect URIs לא זהה לזו
ש-Supabase מציגה. חייבת להיות זהה תו-בתו, כולל `https://` ובלי `/` בסוף.

**מתחבר וחוזר למסך ההתחברות**
`NEXT_PUBLIC_SITE_URL` לא תואם לדומיין האמיתי, או שהכתובת חסרה תחת Redirect
URLs בשלב 5.3. אחרי תיקון — Redeploy.

**"Access blocked" מגוגל**
מסך ההסכמה ב-Testing והמייל שלך לא ברשימת ה-Test users. חזור לשלב 2.2.

**הלינק פותח מסך ריק אצל החברים**
הרצת את `schema.sql` במלואו? בדוק ב-Table Editor שהטבלה `groups` קיימת.

**"function public_group does not exist"**
ה-SQL רץ חלקית. הרץ את הקובץ שוב מההתחלה — הוא בנוי כך שאפשר להריץ אותו
פעמיים בלי נזק.

**הטבלה לא זזה אצל הצופים בזמן אמת**
רענן אצלם פעם אחת. אם עדיין לא — פתח קונסול בדפדפן שלהם ותראה אם יש שגיאת
WebSocket. רשתות ארגוניות חוסמות לפעמים.

**אחרי כמה שבועות הכל מפסיק לעבוד**
Supabase משהה פרויקטים חינמיים אחרי 7 ימים בלי פעילות. בפרויקט כבר יש
`vercel.json` שמריץ פינג שבועי אוטומטי, אז זה לא אמור לקרות. אם בכל זאת —
היכנס ל-Supabase ולחץ **Restore project**. הנתונים לא הולכים לאיבוד.

---

# תחזוקה שוטפת

**גיבוי.** התוכנית החינמית של Supabase לא שומרת גיבויים. פעם בכמה חודשים
לחץ **ייצוא** באפליקציה ושמור את הקובץ. לוקח עשר שניות ומכסה כל תרחיש.

**החלפת לינק שדלף.** ב-Supabase → SQL Editor:

```sql
update groups set slug = 'משהו-חדש-ואקראי' where slug = 'הישן';
```

הלינק הקודם מת מיד. שלח לקבוצה את החדש.
